language: ruby
os:
- linux
dist: trusty
sudo: true
services:
- docker
cache:
  directories:
  - "$HOME/.cache/"
before_install:
- docker --version
- sudo apt-get update
- sudo apt-get install python3
- pip install awscli --upgrade --user
- export PATH=$PATH:$HOME/.local/bin

script:
  true
